import Assistant from "./Assistant";

// The Marketing Assistant lives inside the signed-in app, so it inherits the noindex from
// app/app/layout.tsx.

export default function AssistantPage() {
  return <Assistant />;
}
